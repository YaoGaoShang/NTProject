替换模型



3.25   
	更换UI界面


3.27

	更换模型和标线
	数据持久化已经做好了
	预览场景已经ok

3.28  
	更换模型  ，超级feel的 模型
	换模型的时候需要注意，编辑场景和预览场景的模型位置必须保持一致，
	同时需要 吧模型的地面加上Group  Layer  和 BoxCollider     加上Tag（Build ）




1、添加模型方法：  需要把模型的名字改成一致的，因为这个是按照模型的名字加载的；
2、之后改拖拽模型按钮的text名字为模型的名字；
3、json文件中添加 模型的属性，名字为模型的名字
4、模型要加BoxCollider   ，layer改为Drag


4.2
	为模型添加高亮显示
4.9
	UI界面更改
	增加了模型的删除功能
4.15
	增加了 热力图场景，并且实现了移动  wasd


4.16   
	 更改了  存储数据   
	用了webservice的存储

5.20 
	 添加了 FbxExport  dll  可以实现保存模型到Application.DataPath  
	修复了 地面 和墙的 UV问题
		

5.23  
	此版本  加了额外的 绘制 加载  保存功能，
	解决的bug：
		
		当绘制一条墙的时候  墙会消失，这是因为wall 身上加了的destroy脚本 去掉就好了

5.23.2
	修改 ：
		添加了load 的时候 为每个墙面还有  地面加了碰撞体，改了热力图加载出来之后的资源
	出现bug：
		点击加载按钮出现，又生成一个wall的模型在Resources文件夹
		当如果 已经存在墙的话，在保存的时候回再次添加，这不是我要的效果，我要的效果是覆盖上次保存的

	解决： 当生点击加载按钮时  执行的时loadBtn 函数，此时为wall 动态添加了一个    wall.AddComponent<ExportModel>();，
		所以才会出现点击按钮 又会克隆wall




